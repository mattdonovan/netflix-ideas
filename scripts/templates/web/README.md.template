# {{PROTOTYPE_TITLE}}

Generated by `npm run build-idea --name={{PROTOTYPE_SLUG}} --title="{{PROTOTYPE_TITLE}}" --web-ui` on {{GENERATED_DATE}}.

**Route:** [`/{{PROTOTYPE_SLUG}}`](http://localhost:5173/{{PROTOTYPE_SLUG}})

## What this is

A web-style scaffold — plain MUI on the Hawkins token surface, no TvFrame, no focus engine. Use this shape for desktop-shaped prototypes (marketing pages, dashboards, web app flows).

## Files

- `{{PROTOTYPE_COMPONENT}}.tsx` — the top-level component. Replace its body with the actual prototype.
- `index.ts` — barrel export.

## Where it's registered

- Picker tile + route: `src/prototypes/registry.tsx` (auto-appended)
- Picker tile rendering: `src/App.tsx` → `<RegistryTile>` (one uniform shape for generated prototypes)
